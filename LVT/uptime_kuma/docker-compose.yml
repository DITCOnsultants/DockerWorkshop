---
services:
  uptime-kuma:
    image: louislam/uptime-kuma:latest
    container_name: uptime-kuma
    ports:
      - "3001:3001"
    volumes:
      - /opt/docker/kuma/data:/app/data
      - /var/run/docker.sock:/var/run/docker.sock
    restart: always
